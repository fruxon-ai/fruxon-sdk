# History

## 0.2.0 (2026-03-06)

* Add `fruxon export` CLI command for consolidating multi-file Python agents into a single file.
* AST-based local import tracing — works with any Python framework (LangChain, CrewAI, Google ADK, etc.).
* `--copy` flag for clipboard output, `--output` flag for file output.

## 0.1.0 (2025-09-26)

* First release on PyPI.
